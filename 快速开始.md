# 快速开始指南

## 🚀 1. 打开项目

```bash
cd /Users/momo/Desktop/SimpleBacktraceDemo
open SimpleBacktraceDemo.xcodeproj
```

或者直接在 Finder 中双击 `SimpleBacktraceDemo.xcodeproj`

## 📱 2. 运行项目

### 方式 1：使用模拟器（推荐）

1. 在 Xcode 顶部选择目标设备：**iPhone 14 Pro** (或其他模拟器)
2. 点击左上角的 **Run** 按钮（或按 ⌘R）
3. 等待编译和启动

### 方式 2：使用真机

1. 连接 iPhone 到 Mac
2. 选择你的设备
3. 配置签名：
   - 点击项目 → **Signing & Capabilities**
   - 勾选 **Automatically manage signing**
   - 选择你的 **Team**
4. 点击 **Run**

## 🎮 3. 测试功能

启动后你会看到 4 个按钮：

### 🔵 按钮 1：获取当前线程堆栈

- **功能**：使用 `backtrace()` 获取当前线程的调用栈
- **原理**：POSIX 标准 API，简单快速
- **用途**：适合获取自己所在线程的堆栈

**点击后输出示例：**
```
📚 堆栈信息：
═══════════════════════════════════════
 0  0x0000000102a3c4d8  SimpleBacktraceDemo  -[ViewController captureCurrentThreadStack] + 52
 1  0x00000001a8b2c3f0  UIKitCore  -[UIApplication sendAction:to:from:forEvent:] + 96
 2  0x00000001a8b34a28  UIKitCore  -[UIControl sendAction:to:forEvent:] + 64
 3  0x00000001a8b35234  UIKitCore  -[UIControl _sendActionsForEvents:withEvent:] + 88
 ...
```

### 🟠 按钮 2：模拟卡顿，获取主线程堆栈

- **功能**：模拟主线程卡顿，从子线程捕获主线程堆栈
- **原理**：使用 Mach API (`thread_suspend` + `thread_get_state` + FP 遍历)
- **用途**：**这就是 Matrix 卡顿检测的核心原理！**

**工作流程：**
1. 主线程睡眠 2 秒（模拟卡顿）
2. 子线程监控并捕获主线程堆栈
3. 显示卡顿时主线程的调用栈

**输出示例：**
```
🔄 模拟主线程卡顿 2 秒...

📸 从子线程捕获到的主线程堆栈：
📚 堆栈信息：
═══════════════════════════════════════
 0  0x00000001b8e7c890  libsystem_kernel.dylib  __semwait_signal + 8
 1  0x00000001b8da4567  libsystem_c.dylib  nanosleep + 200
 2  0x0000000102a3c890  SimpleBacktraceDemo  -[ViewController simulateLagAndCapture] + 124
 ...
```

**关键点：**
- ✅ 成功定位到 `simulateLagAndCapture` 方法
- ✅ 这就是卡顿的"罪魁祸首"
- ✅ Matrix 就是用这个原理检测主线程卡顿

### 🟢 按钮 3：测试多层函数调用

- **功能**：测试嵌套函数调用的堆栈追踪
- **调用链**：`level4 → level3 → level2 → level1 → testNestedCalls`
- **用途**：验证堆栈遍历的完整性

**输出示例：**
```
📸 在第4层函数中捕获的堆栈：
📚 堆栈信息：
═══════════════════════════════════════
 0  0x0000000102a3c4d8  SimpleBacktraceDemo  -[ViewController level4] + 52
 1  0x0000000102a3c890  SimpleBacktraceDemo  -[ViewController level3] + 12
 2  0x0000000102a3c456  SimpleBacktraceDemo  -[ViewController level2] + 12
 3  0x0000000102a3c123  SimpleBacktraceDemo  -[ViewController level1] + 12
 4  0x0000000102a3c000  SimpleBacktraceDemo  -[ViewController testNestedCalls] + 24
 ...

✅ 可以看到完整的调用链：level4 → level3 → level2 → level1
```

### 🔴 按钮 4：清空输出

清空显示区域，方便重新测试。

## 📊 4. 理解输出格式

每行堆栈信息的格式：
```
索引  地址              镜像名                  符号名 + 偏移
 0   0x0102a3c4d8   SimpleBacktraceDemo   -[ViewController method] + 52
```

**字段说明：**
- **索引**：0 = 最内层（当前位置），数字越大越接近栈底
- **地址**：虚拟内存地址（ASLR 后的运行时地址）
- **镜像名**：动态库或主程序名（如 UIKitCore、SimpleBacktraceDemo）
- **符号名**：函数名或方法名
- **偏移**：在函数内的字节偏移（+52 = 函数入口 + 52 字节）

## 🔍 5. 核心代码解析

### 获取当前线程堆栈（简单）

```objc
// 使用 POSIX backtrace API
NSArray<BacktraceFrame *> *frames = 
    [SimpleBacktrace captureCurrentThreadBacktrace:50 skipFrames:2];
```

### 获取其他线程堆栈（完整）

```objc
// 1. 挂起线程
thread_suspend(thread);

// 2. 获取寄存器状态（PC, FP, LR）
_STRUCT_ARM_THREAD_STATE64 threadState;
thread_get_state(thread, ARM_THREAD_STATE64, ...);

// 3. 第一帧：PC（当前执行位置）
buffer[0] = threadState.__pc;

// 4. 后续帧：通过 FP 链表遍历
uintptr_t currentFP = threadState.__fp;
while (currentFP != 0) {
    // 读取栈帧（16 字节）
    FrameEntry frame;
    memcpy(&frame, (void *)currentFP, sizeof(frame));
    
    // 保存返回地址
    buffer[i++] = frame.returnAddress;
    
    // 移动到上一个栈帧
    currentFP = (uintptr_t)frame.previous;
}

// 5. 恢复线程
thread_resume(thread);
```

### 符号化地址

```objc
// 使用 dladdr 将地址转换为符号
Dl_info info;
dladdr((void *)address, &info);

// 获取信息
NSString *imageName = [NSString stringWithUTF8String:info.dli_fname];
NSString *symbolName = [NSString stringWithUTF8String:info.dli_sname];
NSInteger offset = address - (uintptr_t)info.dli_saddr;
```

## 🎓 6. 学习要点

### ARM64 栈帧结构

```
高地址 ↑
┌────────────────┐
│ Return Address │  ← FP + 8 (8 字节) - LR (x30)
├────────────────┤
│ Previous FP    │  ← FP (x29) 指向这里 (FP + 0, 8 字节)
└────────────────┘
低地址 ↓
```

**关键寄存器：**
- **x29 (FP)**: Frame Pointer，指向当前栈帧
- **x30 (LR)**: Link Register，保存返回地址
- **PC**: Program Counter，当前执行的指令
- **SP**: Stack Pointer，栈顶指针

### Matrix 卡顿检测原理

```
1. 启动监控线程（子线程）
   └─ 定期检查主线程状态

2. 检测到卡顿（主线程响应超时）
   └─ 挂起主线程（thread_suspend）
   └─ 获取主线程堆栈（thread_get_state + FP 遍历）
   └─ 恢复主线程（thread_resume）

3. 符号化堆栈
   └─ 运行时符号化（dladdr）或
   └─ 离线符号化（atos + dSYM）

4. 生成报告
   └─ 上报服务器或本地存储
```

## 🐛 7. 常见问题

### Q1: 符号化失败，显示 "???"

**原因：**
- Release 构建可能 strip 了符号表
- 系统库符号缺失

**解决：**
- 使用 Debug 构建
- Build Settings → Strip Debug Symbols During Copy → NO

### Q2: 无法获取主线程堆栈

**原因：**
- 权限问题
- 真机上可能有安全限制

**解决：**
- 使用模拟器测试
- 确保在 Debug 模式

### Q3: 编译错误 "thread_suspend undefined"

**原因：**
- 缺少系统框架

**解决：**
- Xcode 会自动链接系统库，无需手动添加

## 📚 8. 进阶学习

### 相关文档

1. **ARM64堆栈遍历寄存器结构详解.md**
   - 完整的 ARM64 寄存器说明
   - 栈帧内存布局图解
   - 寄存器读取过程详解

2. **Matrix异步堆栈追溯技术实现.md**
   - 异步调用链追踪
   - fishhook 原理

3. **虚拟内存地址与符号化原理.md**
   - ASLR 原理
   - dladdr 和 atos 对比

### 扩展功能实现

想要实现更完整的功能？可以参考：

1. **异步堆栈追溯**
   - Hook `dispatch_async` 等函数
   - 保存异步调用的发起点
   - 合并异步堆栈

2. **离线符号化**
   - 收集 dSYM UUID
   - 使用 atos 批量符号化
   - 生成完整的崩溃报告

3. **性能优化**
   - 缓存符号查询结果
   - 异步处理堆栈符号化
   - 减少主线程开销

## ✅ 9. 验证成功

如果你看到类似下面的输出，说明一切正常：

```
📚 堆栈信息：
═══════════════════════════════════════
 0  0x0000000102a3c4d8  SimpleBacktraceDemo  -[ViewController ...] + 52
 1  0x00000001a8b2c3f0  UIKitCore  -[UIApplication ...] + 96
 ...

✅ 成功获取到当前线程的完整调用栈！
```

**恭喜！你已经掌握了堆栈回溯的核心原理！** 🎉

---

**有任何问题？**
- 查看 `README.md` 获取更多信息
- 阅读源码中的详细注释
- 参考 Matrix 源码深入学习
