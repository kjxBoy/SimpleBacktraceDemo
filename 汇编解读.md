## 汇编解读：常见问答（3 题）

本文整理三类最容易混淆的问题：

- **Q1**：`stp x29, x30, [sp, #-16]!` + `mov x29, sp` 之后，`FP(x29)` 里到底是什么？“当前函数地址”又在哪里？
- **Q3**：理解了 FP/LR 之后，如果想做**堆栈回溯**应该怎么做？是不是“先找到 PC，再开始回溯”？
- **Q2**：**代码地址**与**栈内存地址**有什么本质区别？

---

## Q1：函数序言执行完，`FP(x29)` 里是什么？当前函数地址在哪？

问题原文（精简）：

> `0x100002000: stp x29, x30, [sp, #-16]!`（保存 FP/LR）  
> `0x100002004: mov x29, sp`（设置新 FP）  
> 这两句执行完，`FP` 里存的是调用者（上一个函数）的地址吗？当前函数的地址在哪？


### 结论（最关键）

- **`x29`（FP）存的是“栈内存地址”**：当前函数**栈帧的基址**（frame base），通常等于当时的 `sp`。
- **调用者的“函数入口地址”不会被放进 FP**。这段序言保存的是：
  - **上一帧的 `FP`（旧 `x29`）**：也是一个栈地址
  - **返回地址 `LR`（`x30`）**：代码地址（返回到调用点的下一条指令）
- **“当前函数正在执行哪条指令”用 `PC` 表示**；“当前函数的入口地址（符号起点）”通常由调试器/符号表根据 `PC` 映射得到，**不是靠 FP/LR 直接保存**。
- 更精确的一句话是：
  - **FP 存的是“上一帧的栈帧地址”**（栈地址）
  - **LR（或栈里保存的 LR）存的是“返回到上一帧的代码地址”**（代码地址）
  - **FP 不指向代码；LR 不指向栈**（它们的典型用途分别是“定位栈帧”和“控制流返回”）

### 逐条执行：寄存器与栈的变化

进入 `_myFunction` 时，假设：

```text
x29 = 0x2000F000   // 调用者的 FP（栈地址）
x30 = 0x100001004  // 返回地址（代码地址）
sp  = 0x2000E000
```

#### 第 1 句：保存旧 FP / LR，并为新栈帧腾出空间

```asm
stp x29, x30, [sp, #-16]!
```

等价于：

```text
sp = sp - 16                 -> sp = 0x2000DFF0
[sp]     = old x29 (旧FP)     -> [0x2000DFF0] = 0x2000F000
[sp + 8] = old x30 (返回地址) -> [0x2000DFF8] = 0x100001004
```

此时：**旧 FP 和返回地址已经落栈，但 `x29` 还没更新**。

#### 第 2 句：把 FP 指向当前栈帧基址

```asm
mov x29, sp
```

结果：

```text
x29 = 0x2000DFF0  // 当前函数栈帧基址（栈地址）
```

### 栈帧布局（AArch64 常见约定）

执行完序言后，常见布局是：

```text
高地址
│
│  调用者的栈帧（caller frame）
│
├──────────────────────
│  [x29 + 8] = 保存的 LR（返回地址，代码地址）
│  [x29 + 0] = 保存的上一帧 FP（栈地址）
└── x29 (= 当前帧 FP，通常也等于此时 sp)
低地址
```

### “调用者函数地址”与“当前函数地址”分别在哪？

- **调用者函数入口地址**：通常不会被这段序言存起来。你能稳定拿到的是：
  - **返回地址**（`LR` / `[FP+8]`）：返回到哪里（调用点之后的位置），属于**代码地址**
  - **上一帧 FP**（`[FP]`）：上一帧栈基址，属于**栈地址**
- **当前函数正在执行的指令地址**：概念上是 **`PC`**（你反汇编左侧的 `0x100002000` 就是某条指令的地址）。
- **当前函数入口地址（符号起点）**：一般由调试器根据 `PC` 在符号表中解析得到（例如将 `PC` 映射到某个函数符号范围内）。

### 三类“地址”对照表

| 你关心的东西 | 常见承载位置 | 地址类型 |
| --- | --- | --- |
| 当前执行位置 | `PC`（概念上） | 代码地址 |
| 返回地址 | `LR(x30)` / 栈上 `[FP+8]` | 代码地址 |
| 帧基址 | `FP(x29)` / 栈上保存的上一帧 `FP` | **栈内存地址** |

### 为什么 FP 链式回溯成立？

因为每一帧都保存了：

```text
[FP + 0] = 上一帧 FP
[FP + 8] = 返回地址 LR
```

因此可以沿着 `FP` 一路“向上走”。

更完整、真正可用的堆栈回溯流程（起点 `PC+FP`、第一帧为什么必须用 `PC`、标准 FP 链伪代码、失效场景与替代方案）见 **Q3**。

### 小结（最好记的一句话）

> **FP 解决的是“我在栈里的位置”，不是“函数在哪”。**  
> “我从哪来”看 **LR/返回地址**；“我现在在哪执行”看 **PC**。

---

## Q3：理解了 FP/LR 之后，如何回溯堆栈？是不是“先找到 PC，再开始回溯”？

问题原文（精简）：

> 我理解 FP 存储的是上一个函数、LR 存储的是上一个函数具体的调用地址。那我如果想回溯堆栈，应该怎么做？先找到 PC，然后开始回溯？

### 先把表述纠正到“可实现”的精度

- **FP（x29）存的是“上一帧的栈帧地址”**（栈内存地址），不是“上一个函数的代码地址”
- **LR（x30）存的是“返回到上一帧的代码地址”**（返回到调用点之后的位置）

### 结论：回溯的起点是 `PC + 当前 FP`

更准确的说法不是“先找到 PC 再开始”，而是：

- **当前帧用 `PC` 表示“我现在在哪执行”**
- **当前帧用 `FP` 表示“我的栈帧锚点在哪”**

这两者一起，才能从当前帧开始逐帧回放调用链。

### 标准 ARM64：FP 链式回溯算法（核心）

前提：开启 frame pointer 且函数按 ABI/约定建立栈帧（iOS 默认多数情况下满足，但优化/特殊代码见下文失效场景）。

#### 栈布局约定

```text
[FP + 0] = 上一帧 FP
[FP + 8] = 返回地址（保存的 LR）
```

#### 回溯伪代码（调试器/崩溃回溯工具在做的事）

```c
uintptr_t fp = current_fp;  // x29
uintptr_t pc = current_pc;  // PC（采样/异常时由上下文提供）

while (fp != 0) {
    record_frame(pc); // 符号化：函数名 + offset

    uintptr_t saved_lr = *((uintptr_t *)(fp + 8)); // 返回地址（代码地址）
    uintptr_t prev_fp  = *((uintptr_t *)(fp + 0)); // 上一帧 FP（栈地址）

    pc = saved_lr; // 上一帧“执行点”（更准确是：上一帧调用点之后的位置）
    fp = prev_fp;  // 跳到上一帧
}
```

说明：

- `record_frame(pc)` 的输出通常是 `symbol + offset`（例如 `_myFunction + 0x80`）。
- 真实工程实现会对 `fp` 做更多校验（地址范围、对齐、单调性、可读性等），避免栈损坏时越界读或死循环。

### 为什么第一帧用 PC，而不是 LR？

- **当前函数还没返回**，所以：
  - `PC` 才表示“我现在在哪执行”
  - `LR` 表示“将来要返回到哪里”，**不是当前执行位置**
- 因此：
  - **第一帧用 PC**
  - **后续帧用每一帧栈里保存的 LR**

### 用一个最小例子跑一遍

当前正在 `_myFunction` 内：

```text
PC = 0x100002080   // 正在执行
FP = 0x2000DFF0
```

栈里：

```text
[0x2000DFF0 + 8] = 0x100001004  // saved LR（返回地址）
[0x2000DFF0 + 0] = 0x2000F000   // prev FP（上一帧栈帧基址）
```

回溯一轮：

- 记录：`_myFunction + 0x80`
- 读出 `saved_lr/prev_fp`
- 更新：`pc = 0x100001004`，`fp = 0x2000F000`，进入上一帧继续

### 什么时候这套方法会失效？

常见失效/不完整场景：

- **编译选项省略帧指针**：例如 `-fomit-frame-pointer`
- **leaf function**：可能不建完整栈帧（尤其在较高优化级别）
- **尾调用优化（tail call）**：调用关系会被“折叠”
- **手写汇编 / trampoline**：不按 ABI 建栈帧或破坏约定
- **栈被破坏**：例如 stack smash、越界写导致链断裂

这些场景下通常需要依赖：

- **DWARF / compact unwind**（unwind info）
- **PAC / shadow stack**（新硬件/系统机制相关）
- **采样回溯的栈扫描**（stack scanning）

### 小结（把心智模型定死）

> **PC 告诉我“我在哪”，FP 告诉我“我从哪来”，LR 告诉我“我该回哪”。**  
> 堆栈回溯，就是把这三者按时间倒放。

---

## Q2：代码地址 vs 栈内存地址，有什么区别？

问题原文（精简）：

> 代码地址、栈内存地址这俩有什么区别？

### 一句话定性

- **代码地址**：CPU **要取指/执行的指令**所在的位置（通常在可执行文件映射出来的代码页里）
- **栈内存地址**：CPU **临时数据（局部变量/保存寄存器/参数等）**所在的位置（每个线程一份栈）

### 它们来自不同的内存区域

- **代码地址**：
  - 通常来自 Mach-O 的代码段（常见叫 `__TEXT` 映射的页面）
  - 一般 **可执行、不可写**（W^X 策略）
  - 生命周期：进程运行期间一直存在（映射存在）
- **栈内存地址**：
  - 来自线程的栈内存区（Thread Stack）
  - 一般 **可读写、不可执行**（NX/不可执行栈）
  - 生命周期：随线程存在；其中每个函数栈帧只在调用期间有效

### 对照表（建议背这一张）

| 对比项 | 代码地址 | 栈内存地址 |
| --- | --- | --- |
| 指向什么 | 指令 | 数据 |
| 常见寄存器 | `PC` / `LR` | `SP` / `FP` |
| 是否可写 | 通常 ❌ | ✅ |
| 是否可执行 | ✅ | 通常 ❌ |
| 生命周期 | 进程运行期 | 线程运行期（栈帧是函数调用期） |
| 与回溯关系 | 作为返回地址 | 作为帧链接（FP 链） |

### 为什么容易混？关键在“同一个 64 位数，解释方式不同”

因为它们都长得像 `0x...`。但 CPU 眼里差别取决于**你怎么用它**：

```asm
br  x30        // 把 x30 当“代码地址”跳转
ldr x0, [x29]  // 把 x29 当“内存地址”读数据
```

### 用 C 语言类比（直观）

代码地址（函数指针）：

```c
void foo(void) {}
void (*func_ptr)(void) = foo; // 代码地址
```

栈内存地址（局部变量取地址）：

```c
int x;
int *p = &x; // 栈内存地址（当 x 是栈上的局部变量时）
```

### 小结

> **“地址”只是数值，“用途”才决定它是代码地址还是栈地址。**  
> FP/SP 面向栈；PC/LR 面向控制流（取指/返回）。
